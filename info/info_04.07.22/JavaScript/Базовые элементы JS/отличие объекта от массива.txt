const arr = [1, 2, 3]  --- массив это перечень информации по порядку, каждая сущность имеет строгий номер по порядку.
Порядок имеет коючевую роль. Массив - частный случай объекта.

const obj = {a: 1, b: 2, c: 3}  --- структура хранения данных в парном формате:  ключ -> значение

Получение элемента:

console.log(arr[1]) --- получаю 2 ( 0=1)
console.log(obj[b]) --- получаю 2 
console.log(obj.b)  --- может быть и такой синтаксис, но с некоторых случая может быть проблемма