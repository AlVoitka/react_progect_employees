Синтаксис JQuery


$() --- функция для получения элемента
$(window)  --- получаю элемент по окну браузера
$(document)  --- получаю элемент по дом-дереву
$('class')  ---получаю класс
$("a[href^='#']")  --- получаю ссылку

$('#id') - получение элемента по id

так я получаю нужный мне элемент и далее произвожу действия с ним.

Синтаксис JQuery
Синтаксис JQuery разработан специально для выбора элементов HTML и выполнения некоторых действий над элементами.

Базовый синтаксис: $ (селектор). действие()

Знак $ для определения/доступа к jQuery
A (селектор) для «запроса (или поиска)» HTML-элементов
действиеjQuery (), выполняемое над элементом (ами)
Примеры:

$ (this). Hide ()-скрывает текущий элемент.

$ ("p"). Hide ()-скрывает все <p> элементы.

$ (". Test"). Hide ()-скрывает все элементы с классом = "Test".

$ ("#test"). Hide ()-скрывает элемент с ID = "Test".





Практика:

1. Как всегда при создании реальных скриптов, необходимо удостоверится что дом-структура уже польностью прогрузилась
и если в чистом JS я весь код оборачивал в событие DomContentLoaded, а в Jquery вот так:

$(document).ready(function() {

});

2. Добавляю активность первой кнопке:
- сначала ее получаю по селектору. потом назначаю событие hover и затем включаю классс активности
    $('.list-item:first').hover(function() {
        $(this).toggleClass('active');
    }); 

Примеры использования :first:

$('div:first')	вернет первый div-элемент на странице.
$('.lBox:first')	вернет первый элемент с классом lBox.
$('div[title]:first')	вернет первый div-элемент содержащий атрибут title.


3. Теперь хочу сделать, чтобы при нажатии на третью кнопку скрывались все четные картинки:

$('.list-item:eq(2)').on('click',function(){
        $('.image:even').fadeToggle('slow');
    });

$('.list-item') -  получаю  кнопку по классу и при помощи фильтра  :eq(2) получаю именно третью кнопку
.on('click',function()  - навашиваю Обработчик или источник события click

$('.image; -  обращаюсь ко всем картинкам чарез класс
:even') -  и при помощи простого фильтра я обратился толко ко всем четный элементам ( не забывая пр 0 -это 1)
.fadeToggle('slow') -  использую анимацию библиатеки  и скрываю элемент. 'slow' - медленно. Но можно и д/с поставить

Не забываем что нужно дождаться сборку от webpack!!!!!!!!!!!!!!!!!!!!



4. Теперь пропишу анимацию к пятой кнопке вручную
На сайте во вкладке -карта функций -.animate() много инфы

    $('.list-item:eq(4)').on('click',function(){
        $('.image:odd').animate({
            opacity: 'toggle',
            height: 'toggle',
        },1000);
    });

    :odd' - это нечетные элементы

        animate({  --- все настройки прописываются в объекте
            opacity: 'toggle',
            height: 'toggle',
        },1000);  - время срабатывания анимации